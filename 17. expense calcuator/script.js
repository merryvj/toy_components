const totalEl = document.getElementById("total");
const historyEl = document.getElementById("history");

const entryEl = document.getElementById("entry");

function addEntry() {
    let val = entryEl.value;
    console.log(val);
}